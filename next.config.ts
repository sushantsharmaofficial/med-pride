import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ["images.unsplash.com", "www.medequip-uk.com", "www.unsplash.com"],
  },
};

export default nextConfig;
